# 状态机（State）

**最适合**：有限状态逻辑——订单状态、认证状态、连接生命周期、表单向导、任务队列状态。

## 布局

- 状态 = 圆角矩形（`rx=8`），名称走 `node-name`（sans 14px · 600）；状态子标注按语言选行：中文走 `sublabel-zh`（sans 12px），纯拉丁技术串走 `sublabel-tech`（mono 9px）。
- **起点**：实心 ink 圆点（`r=6`）。**终点**：双环圆点（外圈 `r=8` 描边，内芯 `r=5` 实心）。
- 转移 = 直线或弧线箭头，标签格式 `事件 [守卫] / 动作`（不需要的段省略）。事件词按语言选行：中文事件（提交、通过、驳回）走 arrow-label-zh（sans 12px · 0.12em），纯拉丁事件名（`SUBMIT`、`timeout`）走 arrow-label-latin（mono 8px · 0.06em）；守卫与动作保持半角技术串写法。
- 自循环在状态上方画弧。
- 沿主流程方向排布（左→右或上→下）；宁可重排状态也不要让转移交叉。
- 焦点色落在读者最该注意的那个状态——通常是错误态，或“顺利完结”态。
- 回退 / 撤销类转移用虚线 `4,3`（虚线闭集），与正向流区分；回退弧两端锚状态盒顶中心，弧顶标签遮罩对中弧峰骑线。
- **回退弧收尾对齐**：贝塞尔只在终点垂直，虚线若空段落在收尾处，可见尾段停在斜坡上、与箭头不同轴。保持弧形不动，按弧长数值配 `stroke-dashoffset`，让一段实 dash 盖住末端——虚线尾与箭头同轴（2026-08-23 立，example-state 驳回弧：弧长 353、offset 6.01）。

## 反模式

- 转移数 > 状态数 × 2 → 大概率是两台状态机，拆成两张图。
- “任意状态都可达”的转移逐个状态画线——改用一条注释（如 `超时 → 全部进入错误态`）。
- 没有标签的转移（状态机的全部意义就在“是什么触发了这次跳转”）。

## 示例

- [`assets/example-state.html`](../assets/example-state.html) — 文章生命周期：草稿 → 审核中 → 已发布（焦点）→ 已归档，含驳回回退虚线弧
- [`assets/example-state-dark.html`](../assets/example-state-dark.html) — 深色档（对称换基 α 不动，焦点提档 0.10）
- [`assets/example-state-full.html`](../assets/example-state-full.html) — full 页面级（副题 + 三卡 + 页脚）
